# BungaaCord - Discord-like Voice Chat

Аналог Discord с голосовым чатом, демонстрацией экрана и продвинутым шумодавом.

## Возможности

- 🎤 Голосовой чат с WebRTC
- 📺 Демонстрация экрана
- 🔇 Продвинутый шумодав (как в Discord)
- 🔊 Детектор тишины с настраиваемым порогом
- 💬 Система сообщений с сохранением в SQLite
- 👤 Управление пользователями и администраторами
- 🎛️ Регулировка громкости для каждого участника

## Структура базы данных

### Таблица Users
- `uuid` (TEXT, PRIMARY KEY) - уникальный идентификатор пользователя
- `username` (TEXT, UNIQUE) - имя пользователя
- `is_admin` (BOOLEAN) - флаг администратора

### Таблица Messages
- `id` (INTEGER, PRIMARY KEY AUTOINCREMENT) - идентификатор сообщения
- `type` (TEXT) - тип сообщения (text, media)
- `content` (TEXT) - текст сообщения или путь к файлу
- `datetime` (TEXT) - время создания сообщения
- `user_uuid` (TEXT, FOREIGN KEY) - UUID отправителя

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd bungaacord
```

2. Создайте виртуальное окружение и активируйте его:
```bash
python -m venv .venv
source .venv/bin/activate  # На Windows: .venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` с переменными окружения:
```env
ADMIN_UUID=4698c144-62f9-4d81-ae29-1a4887a98f62
ADMIN_USERNAME=BungaaFACE
```

5. Сгенерируйте SSL сертификаты (если их нет):
```bash
openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes
```

## Запуск

Запустите сервер:
```bash
python server.py
```

Сервер будет доступен по HTTPS на порту 9000. Откройте в браузере:
```
https://localhost:9000?user=YOUR_UUID_HERE
```

**Важно**: Доступ к фронтенду возможен только с валидным UUID пользователя, зарегистрированного в базе данных. Без параметра `user` или с невалидным UUID будет возвращаться ошибка 404.

## Конфигурация

### Ограничение сообщений

В файле [`server.py`](server.py:10) можно настроить максимальное количество сообщений:
```python
MAX_MESSAGES = 20  # Максимальное количество сообщений в базе данных
```

При превышении этого лимита самые старые сообщения будут автоматически удаляться. Если сообщение содержит медиа-файл, файл также будет удален с диска.

### Переменные окружения

- `ADMIN_UUID` - UUID администратора (обязательно)
- `ADMIN_USERNAME` - имя администратора (обязательно)

## API Endpoints

- `GET /` - главная страница
- `GET /ws` - WebSocket для сигнализации
- `GET /health` - проверка здоровья сервера
- `GET /api/messages` - получить последние сообщения
  - Параметры: `limit` (по умолчанию 20)
- `GET /api/users` - получить список пользователей
- `GET /api/user` - получить информацию о пользователе по UUID
  - Параметры: `uuid` (обязательно)
- `GET /admin` - страница администрирования пользователей (только для админов)
  - Параметры: `user` (UUID администратора, обязательный)
- `GET /api/admin/users` - получить список всех пользователей (только для админов)
  - Заголовок: `X-Admin-UUID` (UUID администратора)
- `POST /api/admin/users` - создать нового пользователя (только для админов)
  - Заголовок: `X-Admin-UUID` (UUID администратора)
  - Тело: `{"username": "имя", "uuid": "uuid", "is_admin": false}`

## Использование

1. **Доступ к приложению**:
   - Откройте страницу с параметром `user`, содержащим ваш UUID:
   ```
   https://localhost:9000?user=4698c144-62f9-4d81-ae29-1a4887a98f62
   ```
   - UUID должен быть зарегистрирован в базе данных
   - Администратор добавляется автоматически при первом запуске

2. **Доступ к панели администратора**:
   - Откройте страницу с параметром `user`, содержащим UUID администратора:
   ```
   https://localhost:9000/admin?user=4698c144-62f9-4d81-ae29-1a4887a98f62
   ```
   - Доступ только для пользователей с флагом `is_admin = True`
   - Позволяет создавать новых пользователей и управлять существующими

2. **Присоединение к комнате**:
   - Введите имя пользователя и название комнаты
   - Нажмите "Присоединиться к комнате"
   - Разрешите доступ к микрофону

2. **Управление микрофоном**:
   - Кнопка "Выключить микрофон" - отключает/включает микрофон
   - Кнопка "Заглушить звук" - отключает звук и микрофон

3. **Демонстрация экрана**:
   - Нажмите "Начать демонстрацию"
   - Выберите окно или весь экран для демонстрации
   - Используйте элементы управления для управления громкостью и полноэкранным режимом

4. **Шумодав**:
   - Три режима: Минимальный, Умеренный, Агрессивный
   - Возможность отключения/включения
   - Переанализировать фоновый шум при необходимости

5. **Детектор тишины**:
   - Настраиваемый порог громкости
   - Автоматическое отключение микрофона при тишине
   - Визуальный индикатор громкости

## Структура проекта

```
bungaacord/
├── server.py              # Основной серверный файл
├── database.py            # Модуль работы с базой данных
├── requirements.txt       # Зависимости Python
├── .env                   # Переменные окружения
├── .gitignore            # Git ignore файл
├── cert.pem              # SSL сертификат
├── key.pem               # SSL ключ
├── templates/
│   └── index.html        # HTML шаблон
└── static/
    ├── css/
    │   └── styles.css    # Стили
    ├── js/
    │   ├── main.js       # Основной JavaScript
    │   └── noise-suppressor-v2.js  # Шумодав
    └── media/            # Папка для медиа файлов
```

## Особенности реализации

### База данных SQLite

- Автоматическое создание таблиц при запуске
- Добавление администратора из переменных окружения
- Ограничение на количество хранимых сообщений
- Автоматическое удаление старых сообщений и медиа файлов

### WebRTC

- P2P соединения для голосовой связи
- Отдельные соединения для демонстрации экрана
- STUN серверы для NAT traversal

### Шумодав

- Три режима работы: минимальный, умеренный, агрессивный
- Анализ фонового шума
- Возможность переанализа шума

### Безопасность

- HTTPS соединение
- SSL/TLS сертификаты
- WebSocket over SSL
- **Контроль доступа**: Доступ к фронтенду только для зарегистрированных пользователей по UUID
- **Проверка UUID**: Каждый запрос к главной странице проверяет наличие пользователя в базе данных
- **Админ-панель**: Доступ только для администраторов с дополнительной проверкой прав
- **API защита**: Все административные endpoints требуют заголовок с UUID администратора

## Разработка

### Добавление новых возможностей

1. Для добавления новых типов сообщений измените [`database.py`](database.py:1)
2. Для новых WebSocket сообщений измените [`server.py`](server.py:16)
3. Для нового UI функционала измените [`static/js/main.js`](static/js/main.js:1)

## Лицензия

MIT License

## Автор

BungaaFACE